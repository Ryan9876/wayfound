import { randomUUID } from "node:crypto";
import { notFound } from "next/navigation";
import { WorkspaceFrame } from "@/components/workspace-frame";
import { CreateDecisionForm } from "@/components/workspace-forms";
import { workspaceService } from "@/lib/application/workspaces";
import { decisionService } from "@/lib/application/decisions";
import { stageCatalog } from "@/lib/domain/journey";
export const dynamic = "force-dynamic";
export default async function WorkspacePage({ params }: { params: Promise<{ id: string }> }) {
 const { id } = await params;
 const workspace = await (await workspaceService()).open(id);
 if (!workspace) notFound();
 const decisions = await (await decisionService()).list(id);
 const current = stageCatalog.find(s => s.number === workspace.release.current_stage)!;
 return <WorkspaceFrame signedIn><div className="durable-heading"><span className="eyebrow">Saved workspace · Revision {workspace.revision}</span><h1>{workspace.name}</h1><p>{workspace.release.label} <span className="status-chip">{workspace.release.lifecycle}</span></p><span className="stage-pill">Stage {current.number}: {current.name}</span></div><div className="workspace-columns"><section><article className="durable-card durable-next"><span className="eyebrow">Next action · Guidance</span><h2>Clarify who experiences this problem.</h2><p>Describe one situation in which the problem occurs and the outcome that would improve it.</p><div className="entry-note"><strong>Why this matters</strong><p>A clear problem gives the project a useful starting point. Evidence and requirements have not been accepted yet.</p></div></article><article className="durable-card problem-record"><span className="eyebrow">Your problem statement</span><p>{workspace.problem_statement}</p><span className="form-help">Recorded at workspace creation. This statement is not verification evidence.</span></article><section id="decisions" className="decision-section" aria-labelledby="decisions-title"><div className="decision-section-header"><div><span className="eyebrow">Project record</span><h2 id="decisions-title">Decisions</h2><p>These are accepted product-scope or business choices made under product-owner authority.</p></div><span className="status-chip">{decisions.length} accepted</span></div>{decisions.length ? <div className="decision-list">{decisions.map(decision => <article className="durable-card decision-card" key={decision.id}><span className="eyebrow">Accepted decision</span><h3>{decision.title}</h3><p>{decision.decision}</p><div className="entry-note"><strong>Rationale</strong><p>{decision.rationale}</p></div><div className="decision-meta"><span><strong>Status:</strong> {decision.status}</span><span><strong>Authority:</strong> Product owner</span><span><strong>Stage:</strong> {decision.stage_number} · {stageCatalog[decision.stage_number - 1].name}</span><span><strong>Revision:</strong> {decision.revision}</span></div></article>)}</div> : <article className="durable-card"><h3>No accepted decisions yet.</h3><p>Record a product-scope or business choice when you are ready to make it part of project direction.</p></article>}<article className="durable-card decision-form-card"><span className="eyebrow">Product-owner authority</span><h3>Record an accepted decision</h3><p>Use this only for a product-scope or business decision you are authorized to approve. Consequential technical decisions require qualified specialist review.</p><CreateDecisionForm workspaceId={workspace.id} requestId={randomUUID()} /></article></section></section><aside className="durable-card"><span className="eyebrow">Journey</span><h2>A map, not a gate.</h2><ol className="durable-journey">{workspace.stages.map(s => <li key={s.stage_number} aria-current={s.stage_number === current.number ? "step" : undefined}><span className="journey-number">{s.stage_number}</span><span>{stageCatalog[s.stage_number - 1].name}<small>{({active:"Current stage",upcoming:"Upcoming",complete:"Complete",reopened:"Reopened",blocked:"Blocked"} as Record<string,string>)[s.state]}</small></span></li>)}</ol></aside></div></WorkspaceFrame>;
}
